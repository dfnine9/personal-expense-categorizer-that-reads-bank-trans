# personal-expense-categorizer-that-reads-bank-trans
Personal expense categorizer that reads bank transaction CSV files, uses keyword matching and amount patterns to automatically categorize expenses, and outputs spending analysis by category
